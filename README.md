#実装した機能
- 命令
    - ロード命令
        - li    : load immidiate
        - lb(u) : load byte (unsigned)
        - lh(u) : load half (unsigned)
        - lw(u) : load word (unsigned)
    - 算術演算命令
        - add(u, i)
        - sub(u, i)


